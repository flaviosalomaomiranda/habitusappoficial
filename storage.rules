rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return signedIn()
        && request.auth.token.email != null
        && request.auth.token.email in [
          "ndsuporte1@gmail.com",
          "dr.flaviosalomao@hotmail.com"
        ];
    }

    // Perfil e galeria da Rede de Apoio
    match /support-network/profiles/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin()
        && request.resource.size <= 1500000
        && request.resource.contentType.matches('image/.*');
    }

    // Retrocompatibilidade (uploads antigos com caminho /photos)
    match /support-network/photos/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin()
        && request.resource.size <= 1500000
        && request.resource.contentType.matches('image/.*');
    }

    match /support-network/gallery/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin()
        && request.resource.size <= 2000000
        && request.resource.contentType.matches('image/.*');
    }

    match /support-network/videos/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin()
        && request.resource.size <= 20000000
        && request.resource.contentType == 'video/mp4';
    }

    // ✅ fotos de perfil de gerentes
    match /managers/profiles/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin()
        && request.resource.size <= 1500000
        && request.resource.contentType.matches('image/.*');
    }

    // ✅ imagens dos modelos globais de rotina
    match /routine-templates/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin()
        && request.resource.size <= 1500000
        && request.resource.contentType.matches('image/.*');
    }

    // Avatares personalizados de pessoas da familia
    match /families/{familyId}/children-avatars/{allPaths=**} {
      allow read: if signedIn();
      allow write: if signedIn()
        && request.resource.size <= 1000000
        && request.resource.contentType.matches('image/.*');
    }

    // Tudo o resto: somente leitura pública
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
